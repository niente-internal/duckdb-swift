platform :ios, '13.0'
use_frameworks!

# Add this at the top to point to your local podspec
source 'https://github.com/CocoaPods/Specs.git'
# Optional: Add path to folder containing your podspec
pod 'DuckDB-Swift', :path => './path/to/folder/containing/podspec'

target 'YourTargetName' do
  pod 'DuckDB-Swift', :git => 'https://github.com/duckdb/duckdb-swift.git', :branch => 'main'
end 